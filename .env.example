# LocalTunnel Client - Environment Variables Configuration
# Copy this file to .env and configure your settings
# All variables are prefixed with LT_

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# Internal HTTP server port to expose
LT_PORT=3000

# =============================================================================
# BASIC CONFIGURATION
# =============================================================================

# Upstream server providing forwarding
# Default: https://localtunnel.me
LT_HOST=https://localtunnel.me

# Request a specific subdomain (default is random)
# LT_SUBDOMAIN=myapp

# Tunnel traffic to this host instead of localhost
# LT_LOCAL_HOST=localhost

# =============================================================================
# AUTHENTICATION (Protocol 0.0.10-epc) - OPTIONAL
# =============================================================================

# Client Token Authentication (Protocol 0.0.9-epc)
# Use a persistent token to identify your client and reserve your subdomain
# Format: alphanumeric, hyphens, and underscores only [a-zA-Z0-9_-]+
# Maximum length: 256 characters
# Benefits:
#   - Reconnect with same subdomain from different IP addresses
#   - Persistent client identification across network changes
# LT_CLIENT_TOKEN=my-secure-token-123

# HMAC-SHA256 Authentication (Protocol 0.0.10-epc)
# Cryptographically authenticate your tunnel creation requests
# Minimum length: 32 characters (enforced for security)
# Benefits:
#   - Cryptographic authentication of requests
#   - Protection against replay attacks using nonce
#   - Server-side validation of request authenticity
# LT_HMAC_SECRET=my-very-secure-shared-secret-at-least-32-characters-long

# =============================================================================
# LOCAL HTTPS SERVER CONFIGURATION - OPTIONAL
# =============================================================================

# Enable tunneling to a local HTTPS server
# LT_LOCAL_HTTPS=true

# Path to certificate PEM file for local HTTPS server
# LT_LOCAL_CERT=/path/to/cert.pem

# Path to certificate key file for local HTTPS server
# LT_LOCAL_KEY=/path/to/key.pem

# Path to certificate authority file for self-signed certificates
# LT_LOCAL_CA=/path/to/ca.pem

# Disable certificate checks for your local HTTPS server
# Warning: Only use in development/testing environments
# LT_ALLOW_INVALID_CERT=true

# =============================================================================
# USER EXPERIENCE OPTIONS - OPTIONAL
# =============================================================================

# Opens the tunnel URL in your browser automatically
# LT_OPEN=true

# Print basic request info to console
# LT_PRINT_REQUESTS=true

# =============================================================================
# DEBUG SETTINGS - OPTIONAL
# =============================================================================

# Enable debug output for LocalTunnel
# DEBUG=localtunnel:*

# =============================================================================
# SECURITY RECOMMENDATIONS
# =============================================================================

# 1. NEVER commit your .env file to version control
#    Add .env to your .gitignore file
#
# 2. Generate secure tokens:
#    Client Token:
#      openssl rand -base64 32 | tr -d /=+ | cut -c1-32
#
#    HMAC Secret:
#      openssl rand -base64 48
#
# 3. Rotate credentials periodically
#
# 4. Use different credentials for different environments
#    (development, staging, production)
#
# 5. Store production credentials in secure secret management systems
#    (e.g., AWS Secrets Manager, HashiCorp Vault, etc.)

# =============================================================================
# USAGE EXAMPLES
# =============================================================================

# Basic usage (no authentication):
#   LT_PORT=3000 LT_SUBDOMAIN=myapp lt

# With Client Token:
#   LT_PORT=3000 LT_SUBDOMAIN=myapp LT_CLIENT_TOKEN=my-token lt

# With HMAC authentication:
#   LT_PORT=3000 LT_SUBDOMAIN=myapp LT_HMAC_SECRET=my-secret lt

# With both authentication methods (maximum security):
#   LT_PORT=3000 LT_SUBDOMAIN=myapp \
#   LT_CLIENT_TOKEN=my-token \
#   LT_HMAC_SECRET=my-secret lt

# With debug output:
#   DEBUG=localtunnel:* LT_PORT=3000 lt

# For more information, see:
#   - README.md - General usage and API documentation
#   - PROTOCOL_UPGRADE.md - Authentication features guide
#   - examples/authentication-example.js - Practical examples
